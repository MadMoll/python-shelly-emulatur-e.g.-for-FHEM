# python-shelly-emulatur-e.g.-for-FHEM
This is a Shelly EM Gen3 emulator, primery for FHEM, but it makes also sensce to push it in any way via var, while starting the script

Two version, one direkt via var, the other one via MQTT, can used via FHEM but makes not sence.

